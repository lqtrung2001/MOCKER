spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mocker_db
    username: mocker_user
    password: mocker_password
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: mocker.service@gmail.com
    password: bhvlfkcmmrydbntg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true