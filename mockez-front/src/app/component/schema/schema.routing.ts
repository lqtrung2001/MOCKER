import { RouterModule, Routes } from '@angular/router';

/**
 * @author Luong Quoc Trung, Do Quoc Viet
 */

const routes: Routes = [
  // { path: ROUTE.SCHEMA, component: SchemasComponent }
];

export const ROUTING = RouterModule.forChild(routes);
