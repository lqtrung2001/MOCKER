spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mockez_db
    username: mockez_user
    password: mockez_password
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: mockezservice@gmail.com
    password: bhvlfkcmmrydbntg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true